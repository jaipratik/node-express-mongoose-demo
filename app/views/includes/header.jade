header#header
  .navbar.navbar-fixed-top(role='navigation')
    .navbar-inner
      .container
        a.brand(href='/') IndiaDilse
        .nav-collapse.collapse
          ul.nav
            li(class=isActive('/articles/new') ? 'active' : '')
              a(href="/articles/new", title="new article") New
          ul.nav.pull-right
            li


            if (req.isAuthenticated())
              li
                a(href="/users/"+req.user.id, title="Profile") Profile
              li
                a(href="/logout", title="logout") Logout
            else
              li
                a(href="/login", title="Login") Login

  a(href="http://indiadilse.com/")
    img(style="position: fixed; top: 0; right: 0; border: 0; z-index: 9999",
      src="/img/beta.gif",width="30px"
      alt="indiadilse.com")



